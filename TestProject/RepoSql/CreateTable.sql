CREATE TABLE ata_user (
    id integer NOT NULL,
    firstname VARCHAR(255) NOT NULL,
    lastname VARCHAR(255) NOT NULL
);

CREATE TABLE ata_project(
   ID  integer NOT NULL,
   NAME VARCHAR (20) NOT NULL
);